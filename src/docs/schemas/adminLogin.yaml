# Product
components:
  requestBodies:
    AdminLogin:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          format: email
          example: <EMAIL>
        password:
          required: true
          type: string
          example: <PASSWORD>